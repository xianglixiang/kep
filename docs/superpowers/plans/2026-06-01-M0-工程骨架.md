# M0 工程骨架 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭起企业知识管理系统的模块化单体骨架——6 个 Spring Modulith 限界上下文、编译/测试期强制的单向依赖边界、框架级 `tenant_id` 自动过滤、统一异常与返回、Flyway 迁移、对象存储接入；并以端口-适配器隔离 DB 与中间件，提供 `local-mock` 内存模式，使功能测试与本地运行无需 Docker/中间件。

**Architecture:** 单个 Spring Boot 应用，包结构按限界上下文切分（`com.kep.<module>`），用 Spring Modulith 的 `@ApplicationModule(allowedDependencies=...)` + `ApplicationModules.verify()`（底层即 ArchUnit）在测试期阻断非法跨模块依赖。多租户用 Hibernate 6 `@TenantId` + `CurrentTenantIdentifierResolver`，租户过滤下沉到 ORM 层。**所有数据库/中间件访问置于模块内的端口接口之后**：生产用 JPA/MinIO 适配器（`@Profile("!local-mock")`），本地与单测用内存适配器（`@Profile("local-mock")`，亦可直接 `new`）。`shared` 为 OPEN 模块承载横切设施（租户上下文、统一返回、异常、对象存储端口）。

**Tech Stack:** Java 21 · Spring Boot 3.4.1 · Spring Modulith 1.3.1 · Spring Data JPA / Hibernate 6 · PostgreSQL + Flyway · MinIO Java SDK 8.5.x · JUnit 5（标签分层）+ Testcontainers(PostgreSQL, MinIO) · Maven

---

## 设计参数（来自总体架构 spec §0、§6）

- 多租户：共享库 + `tenant_id` 逻辑隔离。
- 模块依赖（严格单向）：`shared`(OPEN) ◄ 所有；`permission` 为地基；`version → permission`；`document → permission, version`；`catalog → permission, document, version`；`review → permission, document, version`；`subscription` 暂为空壳（事件监听在 M2/M5 接入）。
- **端口-适配器 + 测试分层**：
  - DB/中间件均在端口接口之后，生产适配器 `@Profile("!local-mock")`，内存适配器 `@Profile("local-mock")`。
  - **快速测试（默认）**：内存适配器，零 Docker，AI IDE 直接 `./mvnw test`。
  - **集成测试**：`@Tag("integration")`，`./mvnw test -Pit`，Testcontainers 验证 JPA/Hibernate `@TenantId`/Flyway/MinIO 真实接线。
  - **local-mock 运行**：`./mvnw spring-boot:run -Dspring-boot.run.profiles=local-mock` 零中间件启动整个应用。
- M0 验收：① 非法跨模块依赖在测试期 `ApplicationModules.verify()` 失败；② 带 `tenant_id` 的 `catalog_node` 空 CRUD 端到端跑通且跨租户不可见（快速层用内存适配器验证业务语义，集成层用 Testcontainers 验证 Hibernate 框架级过滤）。

## 文件结构（本里程碑创建/修改）

```
pom.xml                                                   构建/依赖/测试分层(Surefire 默认排除 integration tag, -Pit 全跑)
src/main/resources/application.yml                        JPA、Flyway、MinIO 默认配置
src/main/resources/application-local-mock.yml             排除 DB/JPA/Flyway 自动配置
src/main/resources/db/migration/V1__baseline.sql          tenant、catalog_node 基线表
src/main/java/com/kep/KepApplication.java                 启动类（模块根）
src/main/java/com/kep/shared/package-info.java            @ApplicationModule(type = OPEN)
src/main/java/com/kep/shared/web/ApiResponse.java         统一返回封装
src/main/java/com/kep/shared/web/GlobalExceptionHandler.java  统一异常 → ApiResponse
src/main/java/com/kep/shared/error/BusinessException.java     业务异常
src/main/java/com/kep/shared/error/ErrorCode.java             错误码枚举
src/main/java/com/kep/shared/tenant/TenantContext.java        请求级租户上下文(ThreadLocal)
src/main/java/com/kep/shared/tenant/TenantIdentifierResolver.java  Hibernate 当前租户解析器
src/main/java/com/kep/shared/tenant/TenantFilter.java         从 X-Tenant-Id 头填充上下文的 servlet 过滤器
src/main/java/com/kep/shared/jpa/TenantAwareEntity.java       带 @TenantId 的实体基类
src/main/java/com/kep/shared/storage/ObjectStorage.java       对象存储端口
src/main/java/com/kep/shared/storage/MinioObjectStorage.java  生产适配器 @Profile("!local-mock")
src/main/java/com/kep/shared/storage/InMemoryObjectStorage.java  内存适配器 @Profile("local-mock")
src/main/java/com/kep/shared/storage/StorageConfig.java       MinioClient Bean + 配置属性
src/main/java/com/kep/permission/package-info.java        @ApplicationModule(allowedDependencies = {})
src/main/java/com/kep/permission/api/package-info.java    @NamedInterface("api")
src/main/java/com/kep/permission/api/Permission.java      READ/WRITE 枚举
src/main/java/com/kep/permission/api/PermissionChecker.java   鉴权接口（M0 桩实现）
src/main/java/com/kep/permission/internal/AllowAllPermissionChecker.java  M0 桩实现
src/main/java/com/kep/version/package-info.java           allowedDependencies = {"permission :: api"}
src/main/java/com/kep/version/api/package-info.java       @NamedInterface("api")
src/main/java/com/kep/version/api/VersionApi.java         版本接口（M0 占位）
src/main/java/com/kep/document/package-info.java          allowedDependencies = {"permission :: api", "version :: api"}
src/main/java/com/kep/document/api/package-info.java      @NamedInterface("api")
src/main/java/com/kep/document/api/DocumentApi.java       文档接口（M0 占位）
src/main/java/com/kep/catalog/package-info.java           allowedDependencies = {"permission :: api", "document :: api", "version :: api"}
src/main/java/com/kep/catalog/CatalogNode.java            知识目录树实体（M0 演示实体）
src/main/java/com/kep/catalog/CatalogNodeStore.java       端口：目录存取
src/main/java/com/kep/catalog/CatalogNodeRepository.java  Spring Data JPA 仓储
src/main/java/com/kep/catalog/JpaCatalogNodeStore.java    JPA 适配器 @Profile("!local-mock")
src/main/java/com/kep/catalog/InMemoryCatalogNodeStore.java  内存适配器 @Profile("local-mock")
src/main/java/com/kep/catalog/CatalogService.java         目录服务（依赖端口）
src/main/java/com/kep/catalog/CatalogController.java      REST CRUD
src/main/java/com/kep/catalog/dto/CreateNodeRequest.java  入参
src/main/java/com/kep/catalog/dto/NodeView.java           出参
src/main/java/com/kep/review/package-info.java            allowedDependencies = {"permission :: api", "document :: api", "version :: api"}
src/main/java/com/kep/subscription/package-info.java      @ApplicationModule(allowedDependencies = {})
src/test/java/com/kep/ModularityTests.java                Modulith 边界校验（验收①，快速）
src/test/java/com/kep/ContextLoadsLocalMockTest.java      local-mock 零 Docker 启动冒烟（快速）
src/test/java/com/kep/support/IntegrationTest.java        Testcontainers Postgres 基类（@Tag integration）
src/test/java/com/kep/shared/web/GlobalExceptionHandlerTest.java   快速
src/test/java/com/kep/catalog/CatalogServiceTest.java     快速：内存适配器验证业务+租户语义
src/test/java/com/kep/catalog/CatalogControllerLocalMockTest.java  快速：local-mock MockMvc 端到端
src/test/java/com/kep/shared/storage/InMemoryObjectStorageTest.java  快速
src/test/java/com/kep/MigrationSmokeIT.java               集成：Flyway 建表
src/test/java/com/kep/catalog/TenantIsolationIT.java      集成：Hibernate @TenantId 框架级过滤
src/test/java/com/kep/shared/storage/MinioObjectStorageIT.java  集成：MinIO 真实读写
```

> 命名约定：快速测试以 `Test` 结尾、无标签；集成测试以 `IT` 结尾、带 `@Tag("integration")`（经基类或类注解）。Surefire 默认排除 `integration` 组。

---

### Task 1: Maven 工程骨架、测试分层与 local-mock 零依赖启动

**Files:**
- Create: `pom.xml`
- Create: `src/main/java/com/kep/KepApplication.java`
- Create: `src/main/resources/application.yml`
- Create: `src/main/resources/application-local-mock.yml`
- Test: `src/test/java/com/kep/ContextLoadsLocalMockTest.java`

- [ ] **Step 1: 写 `pom.xml`（含测试分层：默认排除 integration，`-Pit` 全跑）**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.4.1</version>
    <relativePath/>
  </parent>

  <groupId>com.kep</groupId>
  <artifactId>kep</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>kep</name>
  <description>企业知识管理系统</description>

  <properties>
    <java.version>21</java.version>
    <spring-modulith.version>1.3.1</spring-modulith.version>
    <minio.version>8.5.14</minio.version>
    <!-- 默认排除集成测试组；-Pit 时清空以全跑 -->
    <test.excluded.groups>integration</test.excluded.groups>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.modulith</groupId>
        <artifactId>spring-modulith-bom</artifactId>
        <version>${spring-modulith.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.modulith</groupId>
      <artifactId>spring-modulith-starter-core</artifactId>
    </dependency>
    <dependency>
      <groupId>io.minio</groupId>
      <artifactId>minio</artifactId>
      <version>${minio.version}</version>
    </dependency>

    <!-- test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>minio</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.modulith</groupId>
      <artifactId>spring-modulith-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <excludedGroups>${test.excluded.groups}</excludedGroups>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>it</id>
      <properties>
        <!-- 清空排除项：快速 + 集成全部运行 -->
        <test.excluded.groups></test.excluded.groups>
      </properties>
    </profile>
  </profiles>
</project>
```

- [ ] **Step 2: 写启动类 `KepApplication.java`**

```java
package com.kep;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class KepApplication {
    public static void main(String[] args) {
        SpringApplication.run(KepApplication.class, args);
    }
}
```

- [ ] **Step 3: 写 `application.yml`（默认/生产剖面）**

```yaml
spring:
  application:
    name: kep
  jpa:
    hibernate:
      ddl-auto: validate          # 表结构由 Flyway 管，JPA 只校验
    open-in-view: false
    properties:
      hibernate.format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

kep:
  storage:
    endpoint: http://localhost:9000
    access-key: minioadmin
    secret-key: minioadmin
    bucket: kep
```

> 默认剖面不配 `spring.datasource.*`：集成测试由 Testcontainers 经 `@ServiceConnection` 注入；部署时补真实数据源。

- [ ] **Step 4: 写 `application-local-mock.yml`（零中间件剖面）**

```yaml
# local-mock：排除 DB/JPA/Flyway 自动配置，配合内存适配器，应用无需任何中间件即可启动。
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.flyway.FlywayAutoConfiguration
```

- [ ] **Step 5: 写快速启动冒烟测试 `ContextLoadsLocalMockTest.java`**

```java
package com.kep;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;

@SpringBootTest
@ActiveProfiles("local-mock")
class ContextLoadsLocalMockTest {

    @Test
    void contextLoads() {
        // local-mock 下应用零中间件启动即通过（无需 Docker）
    }
}
```

- [ ] **Step 6: 运行快速测试确认通过（无需 Docker）**

Run: `./mvnw -q test -Dtest=ContextLoadsLocalMockTest`
Expected: BUILD SUCCESS（不连接任何数据库/中间件）

- [ ] **Step 7: 提交**

```bash
git add pom.xml src/main/java/com/kep/KepApplication.java \
        src/main/resources/application.yml src/main/resources/application-local-mock.yml \
        src/test/java/com/kep/ContextLoadsLocalMockTest.java
git commit -m "chore: Spring Boot 骨架、测试分层与 local-mock 零依赖剖面"
```

---

### Task 2: shared 模块——统一返回与全局异常（快速）

**Files:**
- Create: `src/main/java/com/kep/shared/package-info.java`
- Create: `src/main/java/com/kep/shared/web/ApiResponse.java`
- Create: `src/main/java/com/kep/shared/error/ErrorCode.java`
- Create: `src/main/java/com/kep/shared/error/BusinessException.java`
- Create: `src/main/java/com/kep/shared/web/GlobalExceptionHandler.java`
- Test: `src/test/java/com/kep/shared/web/GlobalExceptionHandlerTest.java`

- [ ] **Step 1: 写失败测试 `GlobalExceptionHandlerTest.java`**

```java
package com.kep.shared.web;

import com.kep.shared.error.BusinessException;
import com.kep.shared.error.ErrorCode;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.context.annotation.Import;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

@WebMvcTest(controllers = GlobalExceptionHandlerTest.BoomController.class)
@Import({GlobalExceptionHandler.class})
class GlobalExceptionHandlerTest {

    @Autowired MockMvc mvc;

    @RestController
    static class BoomController {
        @GetMapping("/boom")
        String boom() {
            throw new BusinessException(ErrorCode.NOT_FOUND, "资源不存在");
        }
    }

    @Test
    void business_exception_is_wrapped_into_api_response() throws Exception {
        mvc.perform(get("/boom"))
           .andExpect(status().isNotFound())
           .andExpect(jsonPath("$.success").value(false))
           .andExpect(jsonPath("$.code").value("NOT_FOUND"))
           .andExpect(jsonPath("$.message").value("资源不存在"));
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `./mvnw -q test -Dtest=GlobalExceptionHandlerTest`
Expected: 编译失败 —— `ApiResponse` / `BusinessException` / `ErrorCode` / `GlobalExceptionHandler` 尚不存在

- [ ] **Step 3: 写 `shared/package-info.java`（OPEN 模块）**

```java
@org.springframework.modulith.ApplicationModule(
    type = org.springframework.modulith.ApplicationModule.Type.OPEN)
package com.kep.shared;
```

> OPEN 模块的所有类型对任意模块开放，横切设施无需在各模块 `allowedDependencies` 中逐一声明。

- [ ] **Step 4: 写 `ErrorCode.java`**

```java
package com.kep.shared.error;

import org.springframework.http.HttpStatus;

public enum ErrorCode {
    BAD_REQUEST(HttpStatus.BAD_REQUEST),
    UNAUTHORIZED(HttpStatus.UNAUTHORIZED),
    FORBIDDEN(HttpStatus.FORBIDDEN),
    NOT_FOUND(HttpStatus.NOT_FOUND),
    CONFLICT(HttpStatus.CONFLICT),
    INTERNAL(HttpStatus.INTERNAL_SERVER_ERROR);

    private final HttpStatus status;

    ErrorCode(HttpStatus status) {
        this.status = status;
    }

    public HttpStatus status() {
        return status;
    }
}
```

- [ ] **Step 5: 写 `BusinessException.java`**

```java
package com.kep.shared.error;

public class BusinessException extends RuntimeException {

    private final ErrorCode code;

    public BusinessException(ErrorCode code, String message) {
        super(message);
        this.code = code;
    }

    public ErrorCode code() {
        return code;
    }
}
```

- [ ] **Step 6: 写 `ApiResponse.java`**

```java
package com.kep.shared.web;

public record ApiResponse<T>(boolean success, String code, String message, T data) {

    public static <T> ApiResponse<T> ok(T data) {
        return new ApiResponse<>(true, "OK", null, data);
    }

    public static <T> ApiResponse<T> error(String code, String message) {
        return new ApiResponse<>(false, code, message, null);
    }
}
```

- [ ] **Step 7: 写 `GlobalExceptionHandler.java`**

```java
package com.kep.shared.web;

import com.kep.shared.error.BusinessException;
import com.kep.shared.error.ErrorCode;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.ExceptionHandler;
import org.springframework.web.bind.annotation.RestControllerAdvice;

@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(BusinessException.class)
    public ResponseEntity<ApiResponse<Void>> handleBusiness(BusinessException ex) {
        return ResponseEntity
            .status(ex.code().status())
            .body(ApiResponse.error(ex.code().name(), ex.getMessage()));
    }

    @ExceptionHandler(Exception.class)
    public ResponseEntity<ApiResponse<Void>> handleUnexpected(Exception ex) {
        return ResponseEntity
            .status(ErrorCode.INTERNAL.status())
            .body(ApiResponse.error(ErrorCode.INTERNAL.name(), "服务器内部错误"));
    }
}
```

- [ ] **Step 8: 运行测试确认通过**

Run: `./mvnw -q test -Dtest=GlobalExceptionHandlerTest`
Expected: BUILD SUCCESS

- [ ] **Step 9: 提交**

```bash
git add src/main/java/com/kep/shared/web/ src/main/java/com/kep/shared/error/ \
        src/main/java/com/kep/shared/package-info.java \
        src/test/java/com/kep/shared/web/GlobalExceptionHandlerTest.java
git commit -m "feat: shared 模块统一返回 ApiResponse 与全局异常处理"
```

---

### Task 3: 框架级租户上下文与 Hibernate 自动过滤（基础设施）

**Files:**
- Create: `src/main/java/com/kep/shared/tenant/TenantContext.java`
- Create: `src/main/java/com/kep/shared/tenant/TenantIdentifierResolver.java`
- Create: `src/main/java/com/kep/shared/tenant/TenantFilter.java`
- Create: `src/main/java/com/kep/shared/jpa/TenantAwareEntity.java`

> 本任务建立租户基础设施；其框架级过滤效果由 Task 7（内存语义，快速）与 Task 8（Hibernate 真实过滤，集成）分别验证。本任务只做编译验证。

- [ ] **Step 1: 写 `TenantContext.java`**

```java
package com.kep.shared.tenant;

public final class TenantContext {

    private static final ThreadLocal<String> CURRENT = new ThreadLocal<>();

    private TenantContext() {}

    public static void set(String tenantId) {
        CURRENT.set(tenantId);
    }

    public static String get() {
        return CURRENT.get();
    }

    public static void clear() {
        CURRENT.remove();
    }
}
```

- [ ] **Step 2: 写 `TenantIdentifierResolver.java`**

```java
package com.kep.shared.tenant;

import org.hibernate.cfg.AvailableSettings;
import org.hibernate.context.spi.CurrentTenantIdentifierResolver;
import org.springframework.boot.autoconfigure.orm.jpa.HibernatePropertiesCustomizer;
import org.springframework.stereotype.Component;

import java.util.Map;

/**
 * 把 TenantContext 的当前租户提供给 Hibernate。
 * 对带 @TenantId 的实体：写入时自动落该值、查询时自动追加 tenant_id 过滤，业务代码无法绕过。
 * 注：local-mock 剖面排除 JPA，本 Bean 存在但不被 Hibernate 使用，无副作用。
 */
@Component
public class TenantIdentifierResolver
        implements CurrentTenantIdentifierResolver<String>, HibernatePropertiesCustomizer {

    /** 无租户上下文时（如系统任务/启动）使用的哨兵值，永不返回 null。 */
    public static final String UNASSIGNED = "__unassigned__";

    @Override
    public String resolveCurrentTenantIdentifier() {
        String tenantId = TenantContext.get();
        return tenantId != null ? tenantId : UNASSIGNED;
    }

    @Override
    public boolean validateExistingCurrentSessions() {
        return false;
    }

    @Override
    public void customize(Map<String, Object> hibernateProperties) {
        hibernateProperties.put(AvailableSettings.MULTI_TENANT_IDENTIFIER_RESOLVER, this);
    }
}
```

- [ ] **Step 3: 写实体基类 `TenantAwareEntity.java`**

```java
package com.kep.shared.jpa;

import jakarta.persistence.Column;
import jakarta.persistence.MappedSuperclass;
import org.hibernate.annotations.TenantId;

/**
 * 所有租户隔离实体的基类。@TenantId 让 Hibernate 在 INSERT 时自动写入当前租户、
 * 在 SELECT 时自动追加 tenant_id 条件。子类无需、也不应手写 tenant 过滤。
 */
@MappedSuperclass
public abstract class TenantAwareEntity {

    @TenantId
    @Column(name = "tenant_id", nullable = false, updatable = false)
    private String tenantId;

    public String getTenantId() {
        return tenantId;
    }
}
```

- [ ] **Step 4: 写请求过滤器 `TenantFilter.java`**

```java
package com.kep.shared.tenant;

import jakarta.servlet.FilterChain;
import jakarta.servlet.ServletException;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.core.annotation.Order;
import org.springframework.stereotype.Component;
import org.springframework.web.filter.OncePerRequestFilter;

import java.io.IOException;

/**
 * 从 X-Tenant-Id 请求头读取租户并写入 TenantContext，请求结束后清理。
 * M0 用请求头承载租户；M1 接入鉴权后改为从已认证主体解析。
 */
@Component
@Order(1)
public class TenantFilter extends OncePerRequestFilter {

    public static final String TENANT_HEADER = "X-Tenant-Id";

    @Override
    protected void doFilterInternal(HttpServletRequest request, HttpServletResponse response,
                                    FilterChain chain) throws ServletException, IOException {
        try {
            String tenantId = request.getHeader(TENANT_HEADER);
            if (tenantId != null && !tenantId.isBlank()) {
                TenantContext.set(tenantId);
            }
            chain.doFilter(request, response);
        } finally {
            TenantContext.clear();
        }
    }
}
```

- [ ] **Step 5: 编译确认无误**

Run: `./mvnw -q test-compile`
Expected: BUILD SUCCESS

- [ ] **Step 6: 提交**

```bash
git add src/main/java/com/kep/shared/tenant/ src/main/java/com/kep/shared/jpa/
git commit -m "feat: 框架级租户上下文与 Hibernate @TenantId 自动过滤"
```

---

### Task 4: Flyway 基线迁移与建表冒烟（集成）

**Files:**
- Create: `src/main/resources/db/migration/V1__baseline.sql`
- Create: `src/test/java/com/kep/support/IntegrationTest.java`
- Test: `src/test/java/com/kep/MigrationSmokeIT.java`

- [ ] **Step 1: 写迁移 `V1__baseline.sql`**

```sql
-- 租户
CREATE TABLE tenant (
    id         VARCHAR(64) PRIMARY KEY,
    name       VARCHAR(200) NOT NULL,
    status     VARCHAR(20)  NOT NULL DEFAULT 'ACTIVE',
    created_at TIMESTAMPTZ  NOT NULL DEFAULT now()
);

-- 知识目录树（M0 演示实体；后续里程碑扩展知识/版本等）
CREATE TABLE catalog_node (
    id          BIGINT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    tenant_id   VARCHAR(64)   NOT NULL,
    parent_id   BIGINT,
    name        VARCHAR(255)  NOT NULL,
    path        VARCHAR(1024) NOT NULL DEFAULT '/',
    sort        INT           NOT NULL DEFAULT 0,
    node_type   VARCHAR(20)   NOT NULL DEFAULT 'FOLDER',  -- FOLDER | KNOWLEDGE
    private     BOOLEAN       NOT NULL DEFAULT FALSE,
    created_at  TIMESTAMPTZ   NOT NULL DEFAULT now()
);

-- tenant_id 必为联合索引首列：多租户逻辑隔离的查询基线
CREATE INDEX idx_catalog_node_tenant_parent ON catalog_node (tenant_id, parent_id);
```

- [ ] **Step 2: 写集成测试基类 `IntegrationTest.java`（带 integration 标签）**

```java
package com.kep.support;

import org.junit.jupiter.api.Tag;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.testcontainers.service.connection.ServiceConnection;
import org.testcontainers.containers.PostgreSQLContainer;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;

/** 集成测试基类：Testcontainers Postgres，默认剖面（启用 JPA/Flyway）。需 Docker。 */
@SpringBootTest
@Testcontainers
@Tag("integration")
public abstract class IntegrationTest {

    @Container
    @ServiceConnection
    static PostgreSQLContainer<?> postgres = new PostgreSQLContainer<>("postgres:16-alpine");
}
```

- [ ] **Step 3: 写迁移冒烟测试 `MigrationSmokeIT.java`**

```java
package com.kep;

import com.kep.support.IntegrationTest;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.core.JdbcTemplate;

import static org.assertj.core.api.Assertions.assertThat;

class MigrationSmokeIT extends IntegrationTest {

    @Autowired JdbcTemplate jdbc;

    @Test
    void flyway_creates_catalog_node_table() {
        Integer count = jdbc.queryForObject(
            "SELECT count(*) FROM information_schema.tables WHERE table_name = 'catalog_node'",
            Integer.class);
        assertThat(count).isEqualTo(1);
    }
}
```

- [ ] **Step 4: 运行集成测试确认通过（需 Docker）**

Run: `./mvnw -q test -Pit -Dtest=MigrationSmokeIT`
Expected: BUILD SUCCESS（Flyway 启动应用 V1，`catalog_node` 存在）。
验证默认不跑：`./mvnw -q test -Dtest=MigrationSmokeIT` 应报 "No tests were executed"（被 integration 标签排除）。

- [ ] **Step 5: 提交**

```bash
git add src/main/resources/db/migration/V1__baseline.sql \
        src/test/java/com/kep/support/IntegrationTest.java \
        src/test/java/com/kep/MigrationSmokeIT.java
git commit -m "feat: Flyway 基线迁移与建表冒烟(集成)"
```

---

### Task 5: permission 地基模块与 PermissionChecker 命名接口（快速）

**Files:**
- Create: `src/main/java/com/kep/permission/package-info.java`
- Create: `src/main/java/com/kep/permission/api/package-info.java`
- Create: `src/main/java/com/kep/permission/api/Permission.java`
- Create: `src/main/java/com/kep/permission/api/PermissionChecker.java`
- Create: `src/main/java/com/kep/permission/internal/AllowAllPermissionChecker.java`

> M0 只立接口与边界，鉴权判定逻辑（三层、就近优先 ACL）在 M1 实现。桩实现放行一切，仅供骨架联通。

- [ ] **Step 1: 写 `permission/package-info.java`**

```java
@org.springframework.modulith.ApplicationModule(
    allowedDependencies = {})
package com.kep.permission;
```

- [ ] **Step 2: 写 `permission/api/package-info.java`（命名接口）**

```java
@org.springframework.modulith.NamedInterface("api")
package com.kep.permission.api;
```

- [ ] **Step 3: 写 `Permission.java`**

```java
package com.kep.permission.api;

public enum Permission {
    READ,
    WRITE
}
```

- [ ] **Step 4: 写 `PermissionChecker.java`**

```java
package com.kep.permission.api;

/**
 * 全系统唯一鉴权入口。各上层模块只经此判定，不自写权限逻辑。
 * M0 为桩实现；M1 落地三层鉴权（租户隔离 / 目录 ACL 就近优先 / 读写分离）。
 */
public interface PermissionChecker {

    void check(Long userId, Long catalogNodeId, Permission required);

    boolean has(Long userId, Long catalogNodeId, Permission required);
}
```

- [ ] **Step 5: 写桩实现 `AllowAllPermissionChecker.java`**

```java
package com.kep.permission.internal;

import com.kep.permission.api.Permission;
import com.kep.permission.api.PermissionChecker;
import org.springframework.stereotype.Service;

/** M0 占位实现：一律放行。M1 替换为真实判定。 */
@Service
class AllowAllPermissionChecker implements PermissionChecker {

    @Override
    public void check(Long userId, Long catalogNodeId, Permission required) {
        // M0：不拦截
    }

    @Override
    public boolean has(Long userId, Long catalogNodeId, Permission required) {
        return true;
    }
}
```

- [ ] **Step 6: 编译确认无误**

Run: `./mvnw -q test-compile`
Expected: BUILD SUCCESS

- [ ] **Step 7: 提交**

```bash
git add src/main/java/com/kep/permission/
git commit -m "feat: permission 地基模块与 PermissionChecker 命名接口(桩)"
```

---

### Task 6: 其余四个模块的边界声明与 Modulith 校验（验收①，快速）

**Files:**
- Create: `src/main/java/com/kep/version/package-info.java`
- Create: `src/main/java/com/kep/version/api/package-info.java`
- Create: `src/main/java/com/kep/version/api/VersionApi.java`
- Create: `src/main/java/com/kep/document/package-info.java`
- Create: `src/main/java/com/kep/document/api/package-info.java`
- Create: `src/main/java/com/kep/document/api/DocumentApi.java`
- Create: `src/main/java/com/kep/review/package-info.java`
- Create: `src/main/java/com/kep/subscription/package-info.java`
- Test: `src/test/java/com/kep/ModularityTests.java`

> catalog 模块的 `package-info.java` 与实现在 Task 7 创建。

- [ ] **Step 1: 写失败测试 `ModularityTests.java`**

```java
package com.kep;

import org.junit.jupiter.api.Test;
import org.springframework.modulith.core.ApplicationModules;

class ModularityTests {

    static final ApplicationModules MODULES = ApplicationModules.of(KepApplication.class);

    @Test
    void verifiesModuleBoundaries() {
        // 违反 allowedDependencies / 访问他模块非命名接口类型时，此处抛异常使构建失败
        MODULES.verify();
    }

    @Test
    void writesDocumentation() {
        MODULES.forEach(System.out::println);
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `./mvnw -q test -Dtest=ModularityTests`
Expected: FAIL —— `version/document/review/subscription` 尚无 `package-info.java`，模块结构不完整或校验报告未声明依赖

- [ ] **Step 3: 写 `version` 模块**

`src/main/java/com/kep/version/package-info.java`:
```java
@org.springframework.modulith.ApplicationModule(
    allowedDependencies = {"permission :: api"})
package com.kep.version;
```

`src/main/java/com/kep/version/api/package-info.java`:
```java
@org.springframework.modulith.NamedInterface("api")
package com.kep.version.api;
```

`src/main/java/com/kep/version/api/VersionApi.java`:
```java
package com.kep.version.api;

/** 版本写入/读取的对外接口。M2 落地历史/diff/回滚；M0 仅占位定义边界。 */
public interface VersionApi {
    // M2 填充：createVersion / listHistory / rollback
}
```

- [ ] **Step 4: 写 `document` 模块**

`src/main/java/com/kep/document/package-info.java`:
```java
@org.springframework.modulith.ApplicationModule(
    allowedDependencies = {"permission :: api", "version :: api"})
package com.kep.document;
```

`src/main/java/com/kep/document/api/package-info.java`:
```java
@org.springframework.modulith.NamedInterface("api")
package com.kep.document.api;
```

`src/main/java/com/kep/document/api/DocumentApi.java`:
```java
package com.kep.document.api;

/** 文档读取的对外接口。M2 落地上传/解析/编辑锁；M0 仅占位定义边界。 */
public interface DocumentApi {
    // M2 填充：getContent / acquireEditLock
}
```

- [ ] **Step 5: 写 `review` 模块 `package-info.java`**

```java
@org.springframework.modulith.ApplicationModule(
    allowedDependencies = {"permission :: api", "document :: api", "version :: api"})
package com.kep.review;
```

- [ ] **Step 6: 写 `subscription` 模块 `package-info.java`**

```java
@org.springframework.modulith.ApplicationModule(
    allowedDependencies = {})
package com.kep.subscription;
```

- [ ] **Step 7: 运行测试确认通过**

Run: `./mvnw -q test -Dtest=ModularityTests`
Expected: BUILD SUCCESS；控制台打印各模块及其允许依赖

- [ ] **Step 8: 验证边界真的会拦截非法依赖（验收①证明）**

临时新建 `src/main/java/com/kep/subscription/IllegalProbe.java`:
```java
package com.kep.subscription;

import com.kep.permission.internal.AllowAllPermissionChecker;  // 非法：访问他模块 internal 类型

class IllegalProbe {
    Class<?> leak = AllowAllPermissionChecker.class;
}
```

Run: `./mvnw -q test -Dtest=ModularityTests#verifiesModuleBoundaries`
Expected: FAIL，报告类似 `Module 'subscription' depends on non-exposed type com.kep.permission.internal.AllowAllPermissionChecker`

> 证明 `verify()`（底层 ArchUnit）在测试期阻断非法跨模块依赖——M0 验收①达成。

- [ ] **Step 9: 删除探针并确认恢复通过**

```bash
rm src/main/java/com/kep/subscription/IllegalProbe.java
```
Run: `./mvnw -q test -Dtest=ModularityTests`
Expected: BUILD SUCCESS

- [ ] **Step 10: 提交**

```bash
git add src/main/java/com/kep/version/ src/main/java/com/kep/document/ \
        src/main/java/com/kep/review/ src/main/java/com/kep/subscription/ \
        src/test/java/com/kep/ModularityTests.java
git commit -m "feat: 声明六模块单向依赖边界并加 Modulith 校验"
```

---

### Task 7: catalog 端口、双适配器与空 CRUD（快速，内存语义验证验收②）

**Files:**
- Create: `src/main/java/com/kep/catalog/package-info.java`
- Create: `src/main/java/com/kep/catalog/CatalogNode.java`
- Create: `src/main/java/com/kep/catalog/CatalogNodeStore.java`
- Create: `src/main/java/com/kep/catalog/CatalogNodeRepository.java`
- Create: `src/main/java/com/kep/catalog/JpaCatalogNodeStore.java`
- Create: `src/main/java/com/kep/catalog/InMemoryCatalogNodeStore.java`
- Create: `src/main/java/com/kep/catalog/dto/CreateNodeRequest.java`
- Create: `src/main/java/com/kep/catalog/dto/NodeView.java`
- Create: `src/main/java/com/kep/catalog/CatalogService.java`
- Create: `src/main/java/com/kep/catalog/CatalogController.java`
- Test: `src/test/java/com/kep/catalog/CatalogServiceTest.java`
- Test: `src/test/java/com/kep/catalog/CatalogControllerLocalMockTest.java`

- [ ] **Step 1: 写失败测试 `CatalogServiceTest.java`（纯内存，零 Spring/Docker）**

```java
package com.kep.catalog;

import com.kep.catalog.dto.CreateNodeRequest;
import com.kep.shared.tenant.TenantContext;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

class CatalogServiceTest {

    private final CatalogService service = new CatalogService(new InMemoryCatalogNodeStore());

    @AfterEach
    void clear() {
        TenantContext.clear();
    }

    @Test
    void create_then_list_returns_node_for_same_tenant() {
        TenantContext.set("tenant-a");
        service.create(new CreateNodeRequest(null, "产品"));

        assertThat(service.listChildren(null))
            .extracting(v -> v.name())
            .containsExactly("产品");
    }

    @Test
    void nodes_are_isolated_between_tenants() {
        TenantContext.set("tenant-a");
        service.create(new CreateNodeRequest(null, "A的根"));

        TenantContext.set("tenant-b");
        service.create(new CreateNodeRequest(null, "B的根"));

        assertThat(service.listChildren(null)).extracting(v -> v.name()).containsExactly("B的根");

        TenantContext.set("tenant-a");
        assertThat(service.listChildren(null)).extracting(v -> v.name()).containsExactly("A的根");
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `./mvnw -q test -Dtest=CatalogServiceTest`
Expected: 编译失败 —— `CatalogService` / `InMemoryCatalogNodeStore` / `CatalogNode` 等尚不存在

- [ ] **Step 3: 写 `catalog/package-info.java`**

```java
@org.springframework.modulith.ApplicationModule(
    allowedDependencies = {"permission :: api", "document :: api", "version :: api"})
package com.kep.catalog;
```

- [ ] **Step 4: 写实体 `CatalogNode.java`（含供内存适配器赋 id 的包级方法）**

```java
package com.kep.catalog;

import com.kep.shared.jpa.TenantAwareEntity;
import jakarta.persistence.*;

@Entity
@Table(name = "catalog_node")
public class CatalogNode extends TenantAwareEntity {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "parent_id")
    private Long parentId;

    @Column(nullable = false)
    private String name;

    @Column(nullable = false)
    private String path = "/";

    @Column(nullable = false)
    private int sort = 0;

    @Column(name = "node_type", nullable = false)
    private String nodeType = "FOLDER";

    // 列名显式指定为 private（privateNode 是 Java 字段名，避开关键字）
    @Column(name = "private", nullable = false)
    private boolean privateNode = false;

    protected CatalogNode() {}

    public static CatalogNode folder(Long parentId, String name, String path) {
        CatalogNode node = new CatalogNode();
        node.parentId = parentId;
        node.name = name;
        node.path = path;
        node.nodeType = "FOLDER";
        return node;
    }

    /** 仅供内存适配器赋 id；JPA 模式下由数据库 IDENTITY 生成。 */
    void assignId(Long id) {
        this.id = id;
    }

    public Long getId() { return id; }
    public Long getParentId() { return parentId; }
    public String getName() { return name; }
    public String getPath() { return path; }
    public int getSort() { return sort; }
    public String getNodeType() { return nodeType; }
    public boolean isPrivateNode() { return privateNode; }
}
```

- [ ] **Step 5: 写端口 `CatalogNodeStore.java`**

```java
package com.kep.catalog;

import java.util.List;

/** 目录存取端口。生产用 JPA 适配器，本地/单测用内存适配器。 */
public interface CatalogNodeStore {

    CatalogNode save(CatalogNode node);

    /** 返回当前租户下指定父节点的子节点，按 sort 升序。 */
    List<CatalogNode> findByParent(Long parentId);
}
```

- [ ] **Step 6: 写 Spring Data 仓储 `CatalogNodeRepository.java`**

```java
package com.kep.catalog;

import org.springframework.data.jpa.repository.JpaRepository;

import java.util.List;

interface CatalogNodeRepository extends JpaRepository<CatalogNode, Long> {

    // 无需写 tenant 条件：@TenantId 由 Hibernate 自动追加
    List<CatalogNode> findByParentIdOrderBySort(Long parentId);
}
```

- [ ] **Step 7: 写 JPA 适配器 `JpaCatalogNodeStore.java`**

```java
package com.kep.catalog;

import org.springframework.context.annotation.Profile;
import org.springframework.stereotype.Component;

import java.util.List;

@Component
@Profile("!local-mock")
class JpaCatalogNodeStore implements CatalogNodeStore {

    private final CatalogNodeRepository repo;

    JpaCatalogNodeStore(CatalogNodeRepository repo) {
        this.repo = repo;
    }

    @Override
    public CatalogNode save(CatalogNode node) {
        return repo.save(node);
    }

    @Override
    public List<CatalogNode> findByParent(Long parentId) {
        return repo.findByParentIdOrderBySort(parentId);
    }
}
```

- [ ] **Step 8: 写内存适配器 `InMemoryCatalogNodeStore.java`（按 TenantContext 隔离）**

```java
package com.kep.catalog;

import com.kep.shared.tenant.TenantContext;
import org.springframework.context.annotation.Profile;
import org.springframework.stereotype.Component;

import java.util.*;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.atomic.AtomicLong;

/**
 * 内存目录存储。用 TenantContext 当前租户作分区键，复刻 Hibernate @TenantId 的隔离语义，
 * 使快速测试与 local-mock 运行无需数据库即反映真实的跨租户不可见行为。
 */
@Component
@Profile("local-mock")
class InMemoryCatalogNodeStore implements CatalogNodeStore {

    private final Map<String, Map<Long, CatalogNode>> byTenant = new ConcurrentHashMap<>();
    private final AtomicLong seq = new AtomicLong();

    private String tenant() {
        String t = TenantContext.get();
        return t != null ? t : "__unassigned__";
    }

    @Override
    public CatalogNode save(CatalogNode node) {
        node.assignId(seq.incrementAndGet());
        byTenant.computeIfAbsent(tenant(), k -> new LinkedHashMap<>()).put(node.getId(), node);
        return node;
    }

    @Override
    public List<CatalogNode> findByParent(Long parentId) {
        return byTenant.getOrDefault(tenant(), Map.of()).values().stream()
            .filter(n -> Objects.equals(n.getParentId(), parentId))
            .sorted(Comparator.comparingInt(CatalogNode::getSort))
            .toList();
    }
}
```

- [ ] **Step 9: 写 DTO `CreateNodeRequest.java` 与 `NodeView.java`**

`CreateNodeRequest.java`:
```java
package com.kep.catalog.dto;

import jakarta.validation.constraints.NotBlank;

public record CreateNodeRequest(Long parentId, @NotBlank String name) {}
```

`NodeView.java`:
```java
package com.kep.catalog.dto;

import com.kep.catalog.CatalogNode;

public record NodeView(Long id, Long parentId, String name, String nodeType) {

    public static NodeView from(CatalogNode node) {
        return new NodeView(node.getId(), node.getParentId(), node.getName(), node.getNodeType());
    }
}
```

> `NodeView.from` 访问 `com.kep.catalog.CatalogNode`，与 DTO 同属 catalog 模块，无跨模块问题。

- [ ] **Step 10: 写服务 `CatalogService.java`（依赖端口）**

```java
package com.kep.catalog;

import com.kep.catalog.dto.CreateNodeRequest;
import com.kep.catalog.dto.NodeView;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

@Service
public class CatalogService {

    private final CatalogNodeStore store;

    public CatalogService(CatalogNodeStore store) {
        this.store = store;
    }

    @Transactional
    public NodeView create(CreateNodeRequest req) {
        CatalogNode saved = store.save(CatalogNode.folder(req.parentId(), req.name(), "/"));
        return NodeView.from(saved);
    }

    @Transactional(readOnly = true)
    public List<NodeView> listChildren(Long parentId) {
        return store.findByParent(parentId).stream().map(NodeView::from).toList();
    }
}
```

> `@Transactional` 在 local-mock 下无事务管理器也安全（无 JPA 时 Spring 不织入事务，方法照常执行）。

- [ ] **Step 11: 写控制器 `CatalogController.java`**

```java
package com.kep.catalog;

import com.kep.catalog.dto.CreateNodeRequest;
import com.kep.catalog.dto.NodeView;
import com.kep.shared.web.ApiResponse;
import jakarta.validation.Valid;
import org.springframework.web.bind.annotation.*;

import java.util.List;

@RestController
@RequestMapping("/api/catalog/nodes")
public class CatalogController {

    private final CatalogService service;

    public CatalogController(CatalogService service) {
        this.service = service;
    }

    @PostMapping
    public ApiResponse<NodeView> create(@Valid @RequestBody CreateNodeRequest req) {
        return ApiResponse.ok(service.create(req));
    }

    @GetMapping
    public ApiResponse<List<NodeView>> list(@RequestParam(required = false) Long parentId) {
        return ApiResponse.ok(service.listChildren(parentId));
    }
}
```

- [ ] **Step 12: 运行内存语义测试确认通过**

Run: `./mvnw -q test -Dtest=CatalogServiceTest`
Expected: BUILD SUCCESS —— 同一服务在不同租户上下文下只见各自数据（内存适配器复刻隔离语义）

- [ ] **Step 13: 写 local-mock HTTP 端到端测试 `CatalogControllerLocalMockTest.java`（零 Docker）**

```java
package com.kep.catalog;

import com.kep.shared.tenant.TenantFilter;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.http.MediaType;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.web.servlet.MockMvc;

import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.post;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

@SpringBootTest
@ActiveProfiles("local-mock")
@AutoConfigureMockMvc
class CatalogControllerLocalMockTest {

    @Autowired MockMvc mvc;

    @Test
    void create_and_list_node_scoped_by_tenant_header() throws Exception {
        mvc.perform(post("/api/catalog/nodes")
                .header(TenantFilter.TENANT_HEADER, "tenant-x")
                .contentType(MediaType.APPLICATION_JSON)
                .content("{\"name\":\"产品\"}"))
           .andExpect(status().isOk())
           .andExpect(jsonPath("$.success").value(true))
           .andExpect(jsonPath("$.data.name").value("产品"));

        mvc.perform(get("/api/catalog/nodes").header(TenantFilter.TENANT_HEADER, "tenant-x"))
           .andExpect(status().isOk())
           .andExpect(jsonPath("$.data.length()").value(1))
           .andExpect(jsonPath("$.data[0].name").value("产品"));

        mvc.perform(get("/api/catalog/nodes").header(TenantFilter.TENANT_HEADER, "tenant-y"))
           .andExpect(status().isOk())
           .andExpect(jsonPath("$.data.length()").value(0));
    }
}
```

- [ ] **Step 14: 运行 local-mock 端到端测试确认通过（零 Docker）**

Run: `./mvnw -q test -Dtest=CatalogControllerLocalMockTest`
Expected: BUILD SUCCESS —— HTTP 头承载租户 → 过滤器 → 上下文 → 内存适配器隔离的全链路成立

- [ ] **Step 15: 提交**

```bash
git add src/main/java/com/kep/catalog/ \
        src/test/java/com/kep/catalog/CatalogServiceTest.java \
        src/test/java/com/kep/catalog/CatalogControllerLocalMockTest.java
git commit -m "feat: catalog 端口+JPA/内存双适配器与空 CRUD(local-mock 快速验证)"
```

---

### Task 8: catalog 集成验证——Hibernate @TenantId 框架级过滤（集成）

**Files:**
- Test: `src/test/java/com/kep/catalog/TenantIsolationIT.java`

> 内存适配器已验证业务语义；本任务用 Testcontainers + 真实 JPA 适配器证明 Hibernate `@TenantId` 自动过滤（框架级、不可绕过）确实生效——这是内存 mock 无法替代的真实接线验证。

- [ ] **Step 1: 写集成测试 `TenantIsolationIT.java`**

```java
package com.kep.catalog;

import com.kep.shared.tenant.TenantContext;
import com.kep.support.IntegrationTest;
import org.junit.jupiter.api.AfterEach;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;

import static org.assertj.core.api.Assertions.assertThat;

/** 默认剖面（启用 JPA），注入真实 JPA 适配器，验证 Hibernate 自动追加 tenant_id 过滤。 */
class TenantIsolationIT extends IntegrationTest {

    @Autowired CatalogNodeStore store;   // 默认剖面下解析为 JpaCatalogNodeStore

    @AfterEach
    void clear() {
        TenantContext.clear();
    }

    @Test
    void hibernate_auto_filters_by_current_tenant() {
        TenantContext.set("tenant-a");
        store.save(CatalogNode.folder(null, "A的根", "/"));

        TenantContext.set("tenant-b");
        store.save(CatalogNode.folder(null, "B的根", "/"));

        assertThat(store.findByParent(null)).extracting(CatalogNode::getName).containsExactly("B的根");

        TenantContext.set("tenant-a");
        assertThat(store.findByParent(null)).extracting(CatalogNode::getName).containsExactly("A的根");
    }
}
```

- [ ] **Step 2: 运行集成测试确认通过（需 Docker）**

Run: `./mvnw -q test -Pit -Dtest=TenantIsolationIT`
Expected: BUILD SUCCESS —— 真实 Postgres 上，同一仓储在不同租户上下文只返回各自数据，证明框架级过滤生效（M0 验收②集成层达成）

- [ ] **Step 3: 提交**

```bash
git add src/test/java/com/kep/catalog/TenantIsolationIT.java
git commit -m "test: 集成验证 Hibernate @TenantId 框架级租户过滤"
```

---

### Task 9: 对象存储端口与双适配器（MinIO / 内存）

**Files:**
- Create: `src/main/java/com/kep/shared/storage/ObjectStorage.java`
- Create: `src/main/java/com/kep/shared/storage/StorageConfig.java`
- Create: `src/main/java/com/kep/shared/storage/MinioObjectStorage.java`
- Create: `src/main/java/com/kep/shared/storage/InMemoryObjectStorage.java`
- Test: `src/test/java/com/kep/shared/storage/InMemoryObjectStorageTest.java`
- Test: `src/test/java/com/kep/shared/storage/MinioObjectStorageIT.java`

- [ ] **Step 1: 写失败测试 `InMemoryObjectStorageTest.java`（快速，零 Docker）**

```java
package com.kep.shared.storage;

import org.junit.jupiter.api.Test;

import java.io.ByteArrayInputStream;
import java.nio.charset.StandardCharsets;

import static org.assertj.core.api.Assertions.assertThat;

class InMemoryObjectStorageTest {

    private final ObjectStorage storage = new InMemoryObjectStorage();

    @Test
    void put_then_get_roundtrips_bytes() {
        byte[] data = "你好,知识库".getBytes(StandardCharsets.UTF_8);
        String key = "tenant-a/docs/hello.txt";

        storage.put(key, new ByteArrayInputStream(data), data.length, "text/plain");

        assertThat(new String(storage.get(key), StandardCharsets.UTF_8)).isEqualTo("你好,知识库");
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `./mvnw -q test -Dtest=InMemoryObjectStorageTest`
Expected: 编译失败 —— `ObjectStorage` / `InMemoryObjectStorage` 尚不存在

- [ ] **Step 3: 写端口 `ObjectStorage.java`**

```java
package com.kep.shared.storage;

import java.io.InputStream;

/** 原始文件（Word/Excel/PPT 等）存储端口。键约定以 tenant_id 为前缀实现租户隔离。 */
public interface ObjectStorage {

    void put(String key, InputStream content, long size, String contentType);

    byte[] get(String key);

    void delete(String key);
}
```

- [ ] **Step 4: 写内存适配器 `InMemoryObjectStorage.java`**

```java
package com.kep.shared.storage;

import com.kep.shared.error.BusinessException;
import com.kep.shared.error.ErrorCode;
import org.springframework.context.annotation.Profile;
import org.springframework.stereotype.Component;

import java.io.IOException;
import java.io.InputStream;
import java.util.Map;
import java.util.concurrent.ConcurrentHashMap;

/** local-mock 下的对象存储：纯内存键值，无需 MinIO。 */
@Component
@Profile("local-mock")
public class InMemoryObjectStorage implements ObjectStorage {

    private final Map<String, byte[]> store = new ConcurrentHashMap<>();

    @Override
    public void put(String key, InputStream content, long size, String contentType) {
        try {
            store.put(key, content.readAllBytes());
        } catch (IOException e) {
            throw new BusinessException(ErrorCode.INTERNAL, "写入内存对象失败: " + e.getMessage());
        }
    }

    @Override
    public byte[] get(String key) {
        byte[] data = store.get(key);
        if (data == null) {
            throw new BusinessException(ErrorCode.NOT_FOUND, "对象不存在: " + key);
        }
        return data;
    }

    @Override
    public void delete(String key) {
        store.remove(key);
    }
}
```

- [ ] **Step 5: 运行内存适配器测试确认通过**

Run: `./mvnw -q test -Dtest=InMemoryObjectStorageTest`
Expected: BUILD SUCCESS

- [ ] **Step 6: 写配置 `StorageConfig.java`**

```java
package com.kep.shared.storage;

import io.minio.MinioClient;
import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Profile;

@Configuration
@EnableConfigurationProperties(StorageConfig.StorageProperties.class)
public class StorageConfig {

    @ConfigurationProperties(prefix = "kep.storage")
    public record StorageProperties(String endpoint, String accessKey, String secretKey, String bucket) {}

    @Bean
    @Profile("!local-mock")
    static MinioClient minioClient(StorageProperties props) {
        return MinioClient.builder()
            .endpoint(props.endpoint())
            .credentials(props.accessKey(), props.secretKey())
            .build();
    }
}
```

- [ ] **Step 7: 写生产适配器 `MinioObjectStorage.java`**

```java
package com.kep.shared.storage;

import com.kep.shared.error.BusinessException;
import com.kep.shared.error.ErrorCode;
import io.minio.*;
import org.springframework.context.annotation.Profile;
import org.springframework.stereotype.Component;

import java.io.InputStream;

@Component
@Profile("!local-mock")
public class MinioObjectStorage implements ObjectStorage {

    private final MinioClient client;
    private final StorageConfig.StorageProperties props;

    public MinioObjectStorage(MinioClient client, StorageConfig.StorageProperties props) {
        this.client = client;
        this.props = props;
        ensureBucket();
    }

    private void ensureBucket() {
        try {
            boolean exists = client.bucketExists(
                BucketExistsArgs.builder().bucket(props.bucket()).build());
            if (!exists) {
                client.makeBucket(MakeBucketArgs.builder().bucket(props.bucket()).build());
            }
        } catch (Exception e) {
            throw new BusinessException(ErrorCode.INTERNAL, "初始化对象存储桶失败: " + e.getMessage());
        }
    }

    @Override
    public void put(String key, InputStream content, long size, String contentType) {
        try {
            client.putObject(PutObjectArgs.builder()
                .bucket(props.bucket()).object(key)
                .stream(content, size, -1)
                .contentType(contentType)
                .build());
        } catch (Exception e) {
            throw new BusinessException(ErrorCode.INTERNAL, "上传对象失败: " + e.getMessage());
        }
    }

    @Override
    public byte[] get(String key) {
        try (InputStream in = client.getObject(
                GetObjectArgs.builder().bucket(props.bucket()).object(key).build())) {
            return in.readAllBytes();
        } catch (Exception e) {
            throw new BusinessException(ErrorCode.NOT_FOUND, "读取对象失败: " + e.getMessage());
        }
    }

    @Override
    public void delete(String key) {
        try {
            client.removeObject(RemoveObjectArgs.builder().bucket(props.bucket()).object(key).build());
        } catch (Exception e) {
            throw new BusinessException(ErrorCode.INTERNAL, "删除对象失败: " + e.getMessage());
        }
    }
}
```

- [ ] **Step 8: 写 MinIO 集成测试 `MinioObjectStorageIT.java`**

```java
package com.kep.shared.storage;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.testcontainers.containers.MinIOContainer;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;

import java.io.ByteArrayInputStream;
import java.nio.charset.StandardCharsets;

import static org.assertj.core.api.Assertions.assertThat;

@Testcontainers
@Tag("integration")
class MinioObjectStorageIT {

    @Container
    static MinIOContainer minio = new MinIOContainer("minio/minio:RELEASE.2024-12-18T13-15-44Z");

    ObjectStorage storage;

    @BeforeEach
    void setUp() {
        var props = new StorageConfig.StorageProperties(
            minio.getS3URL(), minio.getUserName(), minio.getPassword(), "kep-test");
        storage = new MinioObjectStorage(StorageConfig.minioClient(props), props);
    }

    @Test
    void put_then_get_roundtrips_bytes() {
        byte[] data = "你好,知识库".getBytes(StandardCharsets.UTF_8);
        String key = "tenant-a/docs/hello.txt";

        storage.put(key, new ByteArrayInputStream(data), data.length, "text/plain");

        assertThat(new String(storage.get(key), StandardCharsets.UTF_8)).isEqualTo("你好,知识库");
    }
}
```

- [ ] **Step 9: 运行 MinIO 集成测试确认通过（需 Docker）**

Run: `./mvnw -q test -Pit -Dtest=MinioObjectStorageIT`
Expected: BUILD SUCCESS —— 真实 MinIO 上传后取回字节一致

- [ ] **Step 10: 提交**

```bash
git add src/main/java/com/kep/shared/storage/ \
        src/test/java/com/kep/shared/storage/InMemoryObjectStorageTest.java \
        src/test/java/com/kep/shared/storage/MinioObjectStorageIT.java
git commit -m "feat: 对象存储端口与 MinIO/内存双适配器"
```

---

### Task 10: 全量回归与里程碑收尾

**Files:**
- Create: `README.md`

- [ ] **Step 1: 运行快速测试套件（默认，零 Docker）**

Run: `./mvnw -q test`
Expected: BUILD SUCCESS，仅快速用例运行且通过：`ContextLoadsLocalMockTest`、`GlobalExceptionHandlerTest`、`ModularityTests`、`CatalogServiceTest`、`CatalogControllerLocalMockTest`、`InMemoryObjectStorageTest`；集成 IT 被 integration 标签排除。

- [ ] **Step 2: 运行完整套件（含集成，需 Docker）**

Run: `./mvnw -q test -Pit`
Expected: BUILD SUCCESS，追加运行 `MigrationSmokeIT`、`TenantIsolationIT`、`MinioObjectStorageIT` 并通过。

- [ ] **Step 3: 验证 local-mock 零中间件启动应用**

Run: `./mvnw -q spring-boot:run -Dspring-boot.run.profiles=local-mock`（启动后 Ctrl-C 退出）
Expected: 应用正常启动，日志无数据库/MinIO 连接；可手动 `curl -H 'X-Tenant-Id: t1' -X POST localhost:8080/api/catalog/nodes -H 'Content-Type: application/json' -d '{"name":"演示"}'` 返回成功 JSON。

- [ ] **Step 4: 写 `README.md`**

```markdown
# KEP · 企业知识管理系统

模块化单体（Spring Modulith）。当前里程碑：M0 工程骨架。

## 模块边界
- shared(OPEN)：租户上下文、统一返回、异常、对象存储端口
- permission：鉴权地基（M0 桩）
- version / document / catalog / review / subscription：限界上下文（依赖单向，见各 package-info.java）

## 端口-适配器与 local-mock
DB 与中间件均置于端口接口之后：生产用 JPA/MinIO 适配器（`@Profile("!local-mock")`），
本地/单测用内存适配器（`@Profile("local-mock")`）。

## 运行与测试（AI IDE 友好：默认零 Docker）
- 快速测试（内存适配器，无需 Docker）：`./mvnw test`
- 完整测试（含 Testcontainers 集成，需 Docker）：`./mvnw test -Pit`
- 零中间件本地启动：`./mvnw spring-boot:run -Dspring-boot.run.profiles=local-mock`

## 关键验证
- 模块边界：`ModularityTests`（非法跨模块依赖在此失败）
- 租户隔离：`CatalogServiceTest`（内存语义，快速）/ `TenantIsolationIT`（Hibernate 框架级，集成）

## M0 验收
1. 非法跨模块依赖在 `ApplicationModules.verify()` 测试期失败
2. 带 tenant_id 的 catalog_node 空 CRUD 端到端跑通，跨租户不可见（快速层内存验证 + 集成层 Hibernate 验证）
```

- [ ] **Step 5: 提交**

```bash
git add README.md
git commit -m "docs: M0 README 与里程碑收尾"
```

---

## Self-Review（对照 spec §0、§6 与 M0 范围）

- **Spring Boot + Spring Modulith 6 模块空壳** → Task 5（permission）、Task 6（version/document/review/subscription）、Task 7（catalog）。✅
- **ArchUnit 守单向依赖边界** → Task 6 `ModularityTests.verify()`（底层 ArchUnit）+ Step 8/9 显式证明拦截。✅
- **统一异常与返回封装** → Task 2。✅
- **TenantContext + 框架级 tenant_id 自动过滤** → Task 3（基础设施）+ Task 7（内存语义，快速）+ Task 8（Hibernate 真实过滤，集成）。✅
- **对象存储（OSS/MinIO）接入** → Task 9，端口 + MinIO/内存双适配器。✅
- **Flyway 库迁移** → Task 4。✅
- **端口-适配器 + local-mock + 测试分层（用户新增要求 / spec §6）** → Task 1（pom 标签分层 + local-mock 剖面）、Task 7（catalog 端口/双适配器）、Task 9（存储端口/双适配器）、Task 10 Step 3 验证零中间件启动。✅
- **验收① 非法依赖测试期失败** → Task 6 Step 8。✅
- **验收② 带 tenant_id 空 CRUD 跑通** → Task 7 Step 14（快速）+ Task 8 Step 2（集成）。✅
- **类型一致性**：`CatalogNodeStore.save/findByParent` 在端口、JPA 适配器、内存适配器、`CatalogService` 四处签名一致；`CatalogNode.folder(...)`/`assignId`/`getName`/`getSort` 在实体与适配器/测试一致；`ApiResponse.ok/error`、`BusinessException(ErrorCode,String)`、`StorageConfig.StorageProperties(endpoint,accessKey,secretKey,bucket)`、`ObjectStorage.put/get/delete` 在引用处一致；`@Profile("local-mock")` 与 `@Profile("!local-mock")` 成对覆盖 catalog store 与 object storage 两端口。✅
- **占位扫描**：无 TBD/TODO；SQL 用标准 `BIGINT GENERATED ALWAYS AS IDENTITY`，`CatalogNode.privateNode` 显式 `@Column(name = "private")`，代码即写即对。✅
- **测试分层正确性**：快速测试无 `@Tag`/不继承 `IntegrationTest`，集成测试以 `IT` 结尾且带 `@Tag("integration")`（直接或经基类）；Surefire 默认 `excludedGroups=integration`，`-Pit` 清空全跑。✅
- **已知前置**：仅集成测试（`-Pit`）与真实部署需要 Docker；日常开发/功能测试零中间件。
```
