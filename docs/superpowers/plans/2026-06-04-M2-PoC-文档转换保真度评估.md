# M2-PoC 文档转换保真度评估 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用 1 个共享测试 docx 评估 3 个候选 Word→HTML 转换方案（POI 纯 Java / LibreOffice headless / Mammoth 第三方），产出可并排对比的 demo + 评估报告，给 M2-A 选型依据。

**Architecture:** 独立 Maven 子项目 `poc-conversion/`（自带 pom，不进 KEP 主构建）。Spring Boot 后端 + 3 个 REST 端点串行调用 3 个 Converter；前端单 HTML 页面（vanilla JS + Bootstrap CDN）3 列并排 iframe 展示。手工制作 1 份 9 特性覆盖 docx 作为统一测试输入。

**Tech Stack:** Java 21 · Spring Boot 3.4.1 · Apache POI 5.x (XWPF) · JODConverter + LibreOffice headless · Node.js + Mammoth ESM · Bootstrap 5 (CDN) · Maven

---

## 设计参数（来自 M2-PoC spec）

- 范围：1 主格式 (Word) × 3 候选方案 + 轻量 demo
- 评估指标：保真度(40%) + 能力范围(20%) + 性能(20%) + 依赖/运维(20%)
- 产物：可运行 demo + REPORT.md 选型建议
- 端口：18090
- 不进 KEP 主仓构建：独立子项目

## 文件结构

```
poc-conversion/
├── pom.xml                              Spring Boot + POI + JODConverter
├── src/main/java/poc/ConversionController.java
├── src/main/java/poc/converter/Converter.java                (端口接口)
├── src/main/java/poc/converter/PoiDocxToHtml.java            (方案 A)
├── src/main/java/poc/converter/LibreOfficeConverter.java     (方案 B)
├── src/main/java/poc/converter/MammothConverter.java         (方案 C, 调 node)
├── src/main/resources/mammoth/convert.mjs                    (mammoth ESM 包装)
├── src/main/resources/static/index.html                      (3 列对比页)
├── fixtures/test-doc.docx                                    (手工制作 9 特性)
├── src/test/java/poc/converter/PoiDocxToHtmlTest.java        (A 单测)
├── src/test/java/poc/converter/LibreOfficeConverterTest.java (B 单测,需 LO)
├── src/test/java/poc/converter/MammothConverterTest.java     (C 单测,需 Node)
├── src/test/java/poc/ConversionControllerIT.java             (HTTP 端到端)
├── REPORT.md                                                 (评估报告)
└── README.md                                                 (怎么跑)
```

---

### Task 1: 项目骨架 + 转换器端口接口

**Files:**
- Create: `poc-conversion/pom.xml`
- Create: `poc-conversion/src/main/java/poc/converter/Converter.java`
- Create: `poc-conversion/README.md`

- [ ] **Step 1: 写 `poc-conversion/pom.xml`**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.4.1</version>
    <relativePath/>
  </parent>

  <groupId>poc</groupId>
  <artifactId>conversion-poc</artifactId>
  <version>0.0.1-SNAPSHOT</version>

  <properties>
    <java.version>21</java.version>
    <poi.version>5.3.0</poi.version>
    <jodconverter.version>4.4.6</jodconverter.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.poi</groupId>
      <artifactId>poi-ooxml</artifactId>
      <version>${poi.version}</version>
    </dependency>
    <dependency>
      <groupId>org.jodconverter</groupId>
      <artifactId>jodconverter-local</artifactId>
      <version>${jodconverter.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
```

- [ ] **Step 2: 写 `poc-conversion/src/main/java/poc/converter/Converter.java`**

```java
package poc.converter;

import java.io.InputStream;

/** 文档 → HTML 转换端口。3 个方案各自实现。 */
public interface Converter {

    /** 方案名（A/B/C），用于前端标签与报告归类。 */
    String name();

    /** 输入 docx，输出 HTML 字符串。 */
    String convert(InputStream docx) throws Exception;
}
```

- [ ] **Step 3: 写 `poc-conversion/README.md`**

```markdown
# KEP M2-PoC 文档转换保真度评估

Word (.docx) → HTML 的 3 个候选方案对比。

## 依赖

- JDK 21 + Maven
- Node.js 18+ (`mammoth` via npm, 安装在 `src/main/resources/mammoth/node_modules`)
- LibreOffice (`sudo apt install libreoffice` 或 Mac: `brew install --cask libreoffice`)

## 启动

    cd poc-conversion
    npm install --prefix src/main/resources/mammoth   # 安装 mammoth
    ./mvnw spring-boot:run -Dspring-boot.run.arguments=--server.port=18090

打开 http://localhost:18090 上传 docx 即可看 3 方案并排结果。

## 跑测试

    ./mvnw test

## 读报告

[REPORT.md](./REPORT.md)
```

- [ ] **Step 4: 编译验证**

Run: `cd poc-conversion && mvn -q test-compile`
Expected: BUILD SUCCESS

- [ ] **Step 5: 提交**

```bash
cd poc-conversion
mvn -N wrapper:wrapper   # 生成 mvnw（清理可能产生的 D:\mvn_repo 杂目录）
git add poc-conversion/pom.xml poc-conversion/src/main/java/poc/converter/Converter.java poc-conversion/README.md poc-conversion/mvnw poc-conversion/mvnw.cmd poc-conversion/.mvn
git commit -m "chore(m2-poc): 子项目骨架 + Converter 端口"
```

---

### Task 2: 测试 docx（手工 9 特性）

**Files:**
- Create: `poc-conversion/fixtures/test-doc.docx`

> 这步不是代码——需要手工制作一份 docx。提供一个生成脚本让 subagent 用 LibreOffice headless 一次性生成。

- [ ] **Step 1: 写生成脚本 `poc-conversion/fixtures/generate.sh`**

```bash
#!/usr/bin/env bash
# 用 LibreOffice 模板生成；如 LO 不可用，改用 python-docx (Task 2 备选)
set -euo pipefail
OUT=fixtures/test-doc.docx
mkdir -p fixtures

# 用 Python 生成（python-docx 可用的话）
python3 - <<'PY'
from docx import Document
from docx.shared import Inches, Pt, RGBColor
from docx.enum.text import WD_ALIGN_PARAGRAPH
from docx.oxml.ns import qn
from docx.oxml import OxmlElement

doc = Document()

# 1. 标题 1/2/3
doc.add_heading('一级标题：测试文档', level=1)
doc.add_heading('二级标题：保真度评估', level=2)
doc.add_heading('三级标题：M2 PoC', level=3)

# 2. 加粗/斜体/下划线/颜色
p = doc.add_paragraph()
r = p.add_run('这是 ')
r.bold = True
r2 = p.add_run('加粗 ')
r2.italic = True
r3 = p.add_run('下划线 ')
r3.underline = True
r4 = p.add_run('彩色')
r4.font.color.rgb = RGBColor(0xC0, 0x00, 0x00)

# 3. 列表（嵌套 2 层）
doc.add_paragraph('无序列表项 1', style='List Bullet')
doc.add_paragraph('无序列表项 2', style='List Bullet')
sub = doc.add_paragraph('嵌套 1', style='List Bullet')
sub.paragraph_format.left_indent = Inches(0.5)
doc.add_paragraph('有序 1', style='List Number')
doc.add_paragraph('有序 2', style='List Number')

# 4. 表格 3×4 含合并
table = doc.add_table(rows=3, cols=4)
table.style = 'Table Grid'
table.cell(0, 0).text = '表头1'
table.cell(0, 1).text = '表头2'
table.cell(0, 2).text = '表头3'
table.cell(0, 3).text = '表头4'
table.cell(1, 0).merge(table.cell(1, 1)).text = '合并单元格 A+B'
table.cell(1, 2).text = 'C'
table.cell(1, 3).text = 'D'
table.cell(2, 0).text = 'A'
table.cell(2, 1).text = 'B'
table.cell(2, 2).merge(table.cell(2, 3)).text = '合并 C+D'

# 5. 图片（用 1×1 透明 PNG 占位）
png_bytes = bytes.fromhex('89504E470D0A1A0A0000000D49484452000000010000000108060000001F15C4890000000A49444154789C63000100000500010D0A2DB40000000049454E44AE426082')
import io
doc.add_picture(io.BytesIO(png_bytes), width=Inches(2))

# 6. 超链接
doc.add_paragraph('访问 https://example.com 了解更多信息')

# 7. 脚注（python-docx 不直接支持，跳过；LO 仍会渲染）

# 8. 等宽字体（代码块）
code = doc.add_paragraph('def hello(): return "world"')
code.runs[0].font.name = 'Courier New'

# 9. 页眉
section = doc.sections[0]
header = section.header
header.paragraphs[0].text = 'KEP M2-PoC 测试文档'

doc.save('fixtures/test-doc.docx')
print('生成 fixtures/test-doc.docx OK')
PY
```

- [ ] **Step 2: 跑脚本生成 docx**

Run: `cd poc-conversion && pip install python-docx && bash fixtures/generate.sh`
Expected: 输出 "生成 fixtures/test-doc.docx OK"

If python-docx not available, install: `pip install python-docx` (or `pipx install python-docx`).

- [ ] **Step 3: 验证 docx 大小合理（5-50KB）**

Run: `ls -la poc-conversion/fixtures/test-doc.docx`
Expected: 文件存在，~5-30KB

- [ ] **Step 4: 提交（注意：二进制文件正常 commit，git 不压缩 docx）**

```bash
git add poc-conversion/fixtures/test-doc.docx poc-conversion/fixtures/generate.sh
git commit -m "test(m2-poc): 9 特性覆盖测试 docx"
```

---

### Task 3: 方案 A — Apache POI 纯 Java 转换器

**Files:**
- Create: `poc-conversion/src/main/java/poc/converter/PoiDocxToHtml.java`
- Test: `poc-conversion/src/test/java/poc/converter/PoiDocxToHtmlTest.java`

- [ ] **Step 1: 写失败测试 `PoiDocxToHtmlTest.java`**

```java
package poc.converter;

import org.junit.jupiter.api.Test;
import org.springframework.core.io.ClassPathResource;

import java.io.InputStream;

import static org.assertj.core.api.Assertions.assertThat;

class PoiDocxToHtmlTest {

    PoiDocxToHtml converter = new PoiDocxToHtml();

    @Test
    void name() {
        assertThat(converter.name()).isEqualTo("A-POI");
    }

    @Test
    void convert_produces_html_with_headings_paragraphs() throws Exception {
        try (InputStream in = new ClassPathResource("fixtures/test-doc.docx").getInputStream()) {
            String html = converter.convert(in);
            assertThat(html).contains("<h1").contains("测试文档");
            assertThat(html).contains("<h2").contains("保真度");
            assertThat(html).contains("<h3");
        }
    }

    @Test
    void convert_produces_table() throws Exception {
        try (InputStream in = new ClassPathResource("fixtures/test-doc.docx").getInputStream()) {
            String html = converter.convert(in);
            assertThat(html).contains("<table").contains("<td").contains("表头");
        }
    }
}
```

> 测试用 `ClassPathResource("fixtures/test-doc.docx")`——POI 单测要 Maven 把 `fixtures/` 复制到 test classpath 下。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd poc-conversion && ./mvnw test -Dtest=PoiDocxToHtmlTest`
Expected: FAIL — `PoiDocxToHtml` 不存在

- [ ] **Step 3: 写 `PoiDocxToHtml.java`**

```java
package poc.converter;

import org.apache.poi.xwpf.usermodel.*;
import org.springframework.stereotype.Component;

import java.io.InputStream;
import java.util.List;

/**
 * 方案 A：Apache POI 纯 Java。
 * 读取 docx，逐元素输出 HTML。复杂布局（浮动图/分栏/页眉）报 WARN 不实现。
 */
@Component
public class PoiDocxToHtml implements Converter {

    @Override
    public String name() { return "A-POI"; }

    @Override
    public String convert(InputStream docx) throws Exception {
        StringBuilder out = new StringBuilder();
        out.append("<!DOCTYPE html><html><head><meta charset=\"utf-8\"><style>")
           .append("body{font-family:sans-serif;max-width:800px;margin:2em auto;padding:0 1em}")
           .append("table{border-collapse:collapse}td,th{border:1px solid #999;padding:4px 8px}")
           .append("img{max-width:100%}</style></head><body>");

        try (XWPFDocument doc = new XWPFDocument(docx)) {
            // 段落 + 表格按文档顺序遍历
            for (IBodyElement el : doc.getBodyElements()) {
                if (el instanceof XWPFParagraph p) {
                    appendParagraph(p, out);
                } else if (el instanceof XWPFTable t) {
                    appendTable(t, out);
                }
            }
        }
        out.append("</body></html>");
        return out.toString();
    }

    private void appendParagraph(XWPFParagraph p, StringBuilder out) {
        String style = p.getStyle();
        String text = p.getText();
        if (text == null) text = "";

        if (style != null) {
            switch (style) {
                case "Heading1" -> { out.append("<h1>").append(escape(text)).append("</h1>"); return; }
                case "Heading2" -> { out.append("<h2>").append(escape(text)).append("</h2>"); return; }
                case "Heading3" -> { out.append("<h3>").append(escape(text)).append("</h3>"); return; }
                case "ListBullet" -> { out.append("<ul><li>").append(escape(text)).append("</li></ul>"); return; }
                case "ListNumber" -> { out.append("<ol><li>").append(escape(text)).append("</li></ol>"); return; }
                default -> {}
            }
        }
        // 普通段落，含行内 run 样式
        out.append("<p>");
        for (XWPFRun run : p.getRuns()) {
            String r = escape(run.getText(0));
            if (run.isBold()) r = "<b>" + r + "</b>";
            if (run.isItalic()) r = "<i>" + r + "</i>";
            if (run.getUnderline() != null) r = "<u>" + r + "</u>";
            out.append(r);
        }
        out.append("</p>");
    }

    private void appendTable(XWPFTable t, StringBuilder out) {
        out.append("<table>");
        for (XWPFTableRow row : t.getRows()) {
            out.append("<tr>");
            for (XWPFTableCell cell : row.getTableCells()) {
                out.append("<td>").append(escape(cell.getText())).append("</td>");
            }
            out.append("</tr>");
        }
        out.append("</table>");
    }

    private String escape(String s) {
        if (s == null) return "";
        return s.replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;");
    }
}
```

- [ ] **Step 4: 配置 Maven 把 fixtures/ 复制到 test classpath**

修改 `poc-conversion/pom.xml`，在 `<build>` 里加 resources：

```xml
<build>
  <plugins>...</plugins>
  <resources>
    <resource>
      <directory>src/main/resources</directory>
    </resource>
    <resource>
      <directory>fixtures</directory>
    </resource>
  </resources>
  <testResources>
    <testResource>
      <directory>src/test/resources</directory>
    </testResource>
    <testResource>
      <directory>fixtures</directory>
    </testResource>
  </testResources>
</build>
```

- [ ] **Step 5: 跑测试**

Run: `cd poc-conversion && ./mvnw test -Dtest=PoiDocxToHtmlTest`
Expected: PASS, 3 tests

- [ ] **Step 6: 提交**

```bash
cd poc-conversion
git add src/main/java/poc/converter/PoiDocxToHtml.java src/test/java/poc/converter/PoiDocxToHtmlTest.java pom.xml
git commit -m "feat(m2-poc): 方案 A Apache POI 纯 Java 转换器 + 单测"
```

---

### Task 4: 方案 B — LibreOffice headless 转换器

**Files:**
- Create: `poc-conversion/src/main/java/poc/converter/LibreOfficeConverter.java`
- Test: `poc-conversion/src/test/java/poc/converter/LibreOfficeConverterTest.java`

> 此任务需宿主有 LibreOffice。subagent 应先 `which soffice` 探测；若无，标记 BLOCKED 并报告。

- [ ] **Step 1: 探测 LibreOffice**

Run: `which soffice || echo NOT_FOUND`
Expected: `/usr/bin/soffice` 或 `/usr/bin/libreoffice` 或 `NOT_FOUND`

- [ ] **Step 2: 写失败测试 `LibreOfficeConverterTest.java`**

```java
package poc.converter;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.condition.EnabledIf;
import org.springframework.core.io.ClassPathResource;

import java.io.InputStream;

import static org.assertj.core.api.Assertions.assertThat;

@EnabledIf("poc.converter.LibreOfficeConverterTest#sofficeAvailable")
class LibreOfficeConverterTest {

    PoiDocxToHtml dummy = new PoiDocxToHtml();   // 防止 surefire 在没 LO 时报告 "no tests"

    static boolean sofficeAvailable() {
        try {
            return new ProcessBuilder("soffice", "--version").start().waitFor() == 0;
        } catch (Exception e) {
            return false;
        }
    }

    @Test
    void name() {
        assertThat(new LibreOfficeConverter().name()).isEqualTo("B-LibreOffice");
    }

    @Test
    void convert_produces_html_with_table_and_image() throws Exception {
        try (InputStream in = new ClassPathResource("fixtures/test-doc.docx").getInputStream()) {
            String html = new LibreOfficeConverter().convert(in);
            assertThat(html).containsIgnoringCase("<table");
            assertThat(html).containsIgnoringCase("<img");
        }
    }
}
```

- [ ] **Step 3: 写 `LibreOfficeConverter.java`**

```java
package poc.converter;

import org.springframework.stereotype.Component;

import java.io.*;
import java.nio.file.*;

/**
 * 方案 B：LibreOffice headless 转 HTML。
 * 调 {@code soffice --headless --convert-to html --outdir <tmp> <input>};
 * 读取生成的同名 .html 返回。
 * 每次转换起新子进程（PoC 顺序即可；生产用 JODConverter 池化）。
 */
@Component
public class LibreOfficeConverter implements Converter {

    @Override
    public String name() { return "B-LibreOffice"; }

    @Override
    public String convert(InputStream docx) throws Exception {
        Path tmpIn = Files.createTempFile("poc-in-", ".docx");
        Path tmpDir = Files.createTempDirectory("poc-out-");
        try {
            Files.copy(docx, tmpIn, StandardCopyOption.REPLACE_EXISTING);
            ProcessBuilder pb = new ProcessBuilder(
                "soffice", "--headless", "--convert-to", "html",
                "--outdir", tmpDir.toString(), tmpIn.toString());
            pb.redirectErrorStream(true);
            Process p = pb.start();
            String out = new String(p.getInputStream().readAllBytes());
            int code = p.waitFor();
            if (code != 0) {
                throw new RuntimeException("soffice 失败 code=" + code + " out=" + out);
            }
            Path htmlFile = tmpDir.resolve(tmpIn.getFileName().toString().replace(".docx", ".html"));
            return Files.readString(htmlFile);
        } finally {
            Files.deleteIfExists(tmpIn);
            try (var s = Files.list(tmpDir)) {
                s.forEach(p -> { try { Files.deleteIfExists(p); } catch (Exception ignore) {} });
            }
            Files.deleteIfExists(tmpDir);
        }
    }
}
```

- [ ] **Step 4: 跑测试**

Run: `cd poc-conversion && ./mvnw test -Dtest=LibreOfficeConverterTest`
- 若 LibreOffice 可用: PASS
- 若不可用: `@EnabledIf` 跳过（视为"环境不可用"而非失败）
Expected: 至少 1 test (name) 通过；convert 测试可能被跳过

- [ ] **Step 5: 提交**

```bash
cd poc-conversion
git add src/main/java/poc/converter/LibreOfficeConverter.java src/test/java/poc/converter/LibreOfficeConverterTest.java
git commit -m "feat(m2-poc): 方案 B LibreOffice headless 转换器 (@EnabledIf 探测)"
```

---

### Task 5: 方案 C — Mammoth 第三方 + Node 包装

**Files:**
- Create: `poc-conversion/src/main/resources/mammoth/convert.mjs`
- Create: `poc-conversion/src/main/java/poc/converter/MammothConverter.java`
- Test: `poc-conversion/src/test/java/poc/converter/MammothConverterTest.java`

- [ ] **Step 1: 探测 Node + 安装 mammoth**

Run: `which node && node --version && cd poc-conversion && npm install --prefix src/main/resources/mammoth mammoth`
Expected: node 可用 + mammoth 安装到 `src/main/resources/mammoth/node_modules/`

- [ ] **Step 2: 写 Node 脚本 `convert.mjs`**

```javascript
// src/main/resources/mammoth/convert.mjs
// 读 stdin 字节，输出 HTML 到 stdout
import mammoth from "mammoth";
import { readFileSync } from "fs";

const inputPath = process.argv[2];
const buf = readFileSync(inputPath);
const result = await mammoth.convertToHtml({ buffer: buf });
process.stdout.write(result.value);
```

- [ ] **Step 3: 写失败测试 `MammothConverterTest.java`**

```java
package poc.converter;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.condition.EnabledIf;
import org.springframework.core.io.ClassPathResource;

import java.io.InputStream;

import static org.assertj.core.api.Assertions.assertThat;

@EnabledIf("poc.converter.MammothConverterTest#nodeMammothAvailable")
class MammothConverterTest {

    PoiDocxToHtml dummy = new PoiDocxToHtml();

    static boolean nodeMammothAvailable() {
        try {
            return new ProcessBuilder("node", "--version").start().waitFor() == 0
                && new ProcessBuilder("node", "-e", "require.resolve('mammoth')")
                    .directory(new java.io.File("src/main/resources/mammoth"))
                    .start().waitFor() == 0;
        } catch (Exception e) { return false; }
    }

    @Test
    void name() {
        assertThat(new MammothConverter().name()).isEqualTo("C-Mammoth");
    }

    @Test
    void convert_produces_html_with_table() throws Exception {
        try (InputStream in = new ClassPathResource("fixtures/test-doc.docx").getInputStream()) {
            String html = new MammothConverter().convert(in);
            assertThat(html).containsIgnoringCase("<table");
        }
    }
}
```

- [ ] **Step 4: 写 `MammothConverter.java`**

```java
package poc.converter;

import org.springframework.stereotype.Component;

import java.io.*;
import java.nio.file.*;

/**
 * 方案 C：Mammoth（npm）通过 Node 子进程调用。
 * Mammoth 专做 .docx → clean HTML，丢弃样式装饰保语义结构。
 */
@Component
public class MammothConverter implements Converter {

    @Override
    public String name() { return "C-Mammoth"; }

    @Override
    public String convert(InputStream docx) throws Exception {
        Path tmpIn = Files.createTempFile("poc-mammoth-", ".docx");
        try {
            Files.copy(docx, tmpIn, StandardCopyOption.REPLACE_EXISTING);
            ProcessBuilder pb = new ProcessBuilder(
                "node", "convert.mjs", tmpIn.toString());
            pb.directory(new File("src/main/resources/mammoth"));
            pb.redirectErrorStream(true);
            Process p = pb.start();
            String out = p.getInputStream().readAllBytes().toString();
            int code = p.waitFor();
            if (code != 0) {
                throw new RuntimeException("mammoth 失败 code=" + code + " out=" + out);
            }
            return out;
        } finally {
            Files.deleteIfExists(tmpIn);
        }
    }
}
```

> 注：用相对路径 `src/main/resources/mammoth` 在 PoC 跑测试时是相对 CWD。生产用法（M2-A）会改用绝对 classpath 资源路径；PoC 简化用相对。

- [ ] **Step 5: 跑测试**

Run: `cd poc-conversion && ./mvnw test -Dtest=MammothConverterTest`
Expected: PASS (Node + mammoth 可用时), 或被 @EnabledIf 跳过

- [ ] **Step 6: 提交**

```bash
cd poc-conversion
git add src/main/java/poc/converter/MammothConverter.java \
        src/test/java/poc/converter/MammothConverterTest.java \
        src/main/resources/mammoth/convert.mjs \
        src/main/resources/mammoth/package.json \
        src/main/resources/mammoth/package-lock.json
git commit -m "feat(m2-poc): 方案 C Mammoth 转换器 + Node 包装"
```

> 注意：node_modules 不入仓（已加进 .gitignore）。package.json + package-lock.json 入仓保证依赖可复现。

---

### Task 6: HTTP 端到端 Controller + Demo 前端

**Files:**
- Create: `poc-conversion/src/main/java/poc/ConversionController.java`
- Create: `poc-conversion/src/main/resources/static/index.html`
- Create: `poc-conversion/src/main/java/poc/PocApplication.java`
- Test: `poc-conversion/src/test/java/poc/ConversionControllerIT.java`

- [ ] **Step 1: 写 `PocApplication.java`**

```java
package poc;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class PocApplication {
    public static void main(String[] args) {
        SpringApplication.run(PocApplication.class, args);
    }
}
```

- [ ] **Step 2: 写 `ConversionController.java`**

```java
package poc;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.MediaType;
import org.springframework.web.bind.annotation.*;
import org.springframework.web.multipart.MultipartFile;
import poc.converter.Converter;
import poc.converter.LibreOfficeConverter;
import poc.converter.MammothConverter;
import poc.converter.PoiDocxToHtml;

import java.util.LinkedHashMap;
import java.util.Map;

@RestController
@RequestMapping("/convert")
public class ConversionController {

    private final Map<String, Converter> converters = new LinkedHashMap<>();

    @Autowired
    public ConversionController(PoiDocxToHtml a, LibreOfficeConverter b, MammothConverter c) {
        converters.put("a", a);
        converters.put("b", b);
        converters.put("c", c);
    }

    @PostMapping(value = "/{id}", consumes = MediaType.MULTIPART_FORM_DATA_VALUE)
    public Map<String, String> convert(@PathVariable String id, @RequestParam MultipartFile file) throws Exception {
        Converter c = converters.get(id);
        if (c == null) throw new IllegalArgumentException("未知 converter: " + id);
        return Map.of("name", c.name(), "html", c.convert(file.getInputStream()));
    }

    @GetMapping("/all")
    public Map<String, String> allNames() {
        Map<String, String> m = new LinkedHashMap<>();
        converters.forEach((k, v) -> m.put(k, v.name()));
        return m;
    }
}
```

- [ ] **Step 3: 写 `static/index.html`**

```html
<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="utf-8">
  <title>KEP M2-PoC 文档转换保真度对比</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .col-iframe { height: 70vh; border: 1px solid #ccc; background: #fff; }
    .col-header { font-weight: 600; padding: 8px 0; }
  </style>
</head>
<body class="container py-4">
  <h1 class="mb-3">KEP M2-PoC 文档转换保真度对比</h1>
  <p class="text-muted">上传一份 .docx，下面 3 列分别展示 3 个方案的转换结果。</p>

  <div class="mb-4">
    <input type="file" id="file" accept=".docx" class="form-control mb-2">
    <button id="go" class="btn btn-primary">转换</button>
  </div>

  <div class="row g-3" id="results">
    <div class="col-md-4">
      <div class="col-header" id="h-a">A-POI</div>
      <iframe class="col-iframe w-100" id="r-a" sandbox="allow-same-origin"></iframe>
    </div>
    <div class="col-md-4">
      <div class="col-header" id="h-b">B-LibreOffice</div>
      <iframe class="col-iframe w-100" id="r-b" sandbox="allow-same-origin"></iframe>
    </div>
    <div class="col-md-4">
      <div class="col-header" id="h-c">C-Mammoth</div>
      <iframe class="col-iframe w-100" id="r-c" sandbox="allow-same-origin"></iframe>
    </div>
  </div>

  <script>
    const $ = (s) => document.querySelector(s);
    const fileInput = $('#file');
    const btn = $('#go');

    btn.addEventListener('click', async () => {
      const f = fileInput.files[0];
      if (!f) { alert('请先选文件'); return; }
      btn.disabled = true;
      btn.textContent = '转换中…';
      try {
        for (const id of ['a', 'b', 'c']) {
          const fd = new FormData();
          fd.append('file', f);
          const res = await fetch(`/convert/${id}`, { method: 'POST', body: fd });
          if (!res.ok) {
            document.querySelector(`#r-${id}`).srcdoc =
              `<pre style="color:red;padding:1em">HTTP ${res.status}\n${await res.text()}</pre>`;
            continue;
          }
          const json = await res.json();
          document.querySelector(`#h-${id}`).textContent = json.name;
          document.querySelector(`#r-${id}`).srcdoc = json.html;
        }
      } finally {
        btn.disabled = false;
        btn.textContent = '转换';
      }
    });
  </script>
</body>
</html>
```

- [ ] **Step 4: 写 `ConversionControllerIT.java`**

```java
package poc;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.test.web.client.TestRestTemplate;
import org.springframework.core.io.ClassPathResource;
import org.springframework.http.*;
import org.springframework.util.LinkedMultiValueMap;

import java.util.Map;

import static org.assertj.core.api.Assertions.assertThat;

@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
class ConversionControllerIT {

    @Autowired TestRestTemplate rest;

    @Test
    void convert_a_returns_html_with_h1() {
        var headers = new HttpHeaders();
        headers.setContentType(MediaType.MULTIPART_FORM_DATA);

        var body = new LinkedMultiValueMap<String, Object>();
        body.add("file", new ClassPathResource("fixtures/test-doc.docx"));

        var resp = rest.exchange("/convert/a", HttpMethod.POST,
            new HttpEntity<>(body, headers), Map.class);
        assertThat(resp.getStatusCode()).isEqualTo(HttpStatus.OK);
        assertThat(resp.getBody().get("html").toString()).contains("<h1");
    }
}
```

- [ ] **Step 5: 跑测试**

Run: `cd poc-conversion && ./mvnw test -Dtest=ConversionControllerIT`
Expected: PASS

- [ ] **Step 6: 启动 demo 手动验证**

Run:
```bash
cd poc-conversion
./mvnw spring-boot:run -Dspring-boot.run.arguments=--server.port=18090 &
sleep 15
curl -s -X POST -F "file=@fixtures/test-doc.docx" http://localhost:18090/convert/a | head -c 200
pkill -f spring-boot:run
```
Expected: 收到 200 + HTML 包含 `<h1`

- [ ] **Step 7: 提交**

```bash
cd poc-conversion
git add src/main/java/poc/PocApplication.java src/main/java/poc/ConversionController.java \
        src/main/resources/static/index.html \
        src/test/java/poc/ConversionControllerIT.java
git commit -m "feat(m2-poc): HTTP 端到端 Controller + 3 列对比 demo"
```

---

### Task 7: 性能基准 + 评估报告

**Files:**
- Create: `poc-conversion/src/test/java/poc/converter/PerformanceBenchmarkTest.java`
- Create: `poc-conversion/REPORT.md`

- [ ] **Step 1: 写性能基准 `PerformanceBenchmarkTest.java`**

```java
package poc.converter;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.condition.EnabledIfSystemProperty;
import org.springframework.core.io.ClassPathResource;

import java.io.InputStream;
import java.util.ArrayList;
import java.util.List;
import java.util.function.Function;

/**
 * 性能基准：每个方案跑 10 次取 p50/p95。默认禁用（@EnabledIfSystemProperty），
 * 跑：{@code ./mvnw test -Dtest=PerformanceBenchmarkTest -Dbenchmark=true}
 */
@EnabledIfSystemProperty(named = "benchmark", matches = "true")
class PerformanceBenchmarkTest {

    record BenchResult(String name, long p50, long p95, long maxMs) {}

    @Test
    void bench_all_three() throws Exception {
        List<Function<InputStream, String>> fns = List.of(
            in -> { try { return new PoiDocxToHtml().convert(in); } catch (Exception e) { throw new RuntimeException(e); } },
            in -> { try { return new LibreOfficeConverter().convert(in); } catch (Exception e) { throw new RuntimeException(e); } },
            in -> { try { return new MammothConverter().convert(in); } catch (Exception e) { throw new RuntimeException(e); } }
        );
        String[] names = {"A-POI", "B-LibreOffice", "C-Mammoth"};

        for (int i = 0; i < fns.size(); i++) {
            List<Long> times = new ArrayList<>();
            for (int r = 0; r < 10; r++) {
                try (InputStream in = new ClassPathResource("fixtures/test-doc.docx").getInputStream()) {
                    long t0 = System.nanoTime();
                    fns.get(i).apply(in);
                    times.add((System.nanoTime() - t0) / 1_000_000);
                }
            }
            times.sort(Long::compare);
            long p50 = times.get(times.size() / 2);
            long p95 = times.get((int)(times.size() * 0.95));
            long max = times.get(times.size() - 1);
            System.out.printf("[BENCH] %s: p50=%dms p95=%dms max=%dms%n", names[i], p50, p95, max);
        }
    }
}
```

- [ ] **Step 2: 跑基准（手动）**

Run:
```bash
cd poc-conversion
./mvnw test -Dtest=PerformanceBenchmarkTest -Dbenchmark=true 2>&1 | grep "BENCH"
```
Expected: 3 行 BENCH 输出（若 LO/Node 不可用，对应方案可能失败——记录到报告）

- [ ] **Step 3: 写 `REPORT.md`**

模板（**subagent 必须用真实跑出来的数字填入，禁止编造**）：

```markdown
# M2-PoC 文档转换保真度评估报告

日期：2026-06-04

## 1. 概要

- 评估目的：M2-A 文档上传/解析采用哪个 Word→HTML 转换方案
- 测试输入：`fixtures/test-doc.docx`（9 特性覆盖：标题/样式/列表/表格/图片/链接/等宽字体/页眉）
- 评估方法：每个方案对同一 docx 跑 10 次取 p50/p95，9 特性逐项打 ✅/⚠️/❌
- 推荐方案：**A**（填入实际推荐名 + 一句话理由）

## 2. 保真度对照表

| # | 特性 | A-POI | B-LibreOffice | C-Mammoth |
|---|------|-------|---------------|-----------|
| 1 | 标题 1/2/3 | (填) | (填) | (填) |
| 2 | 加粗/斜体/下划线/颜色 | (填) | (填) | (填) |
| 3 | 嵌套列表 | (填) | (填) | (填) |
| 4 | 表格 + 合并单元格 | (填) | (填) | (填) |
| 5 | 嵌入图片 | (填) | (填) | (填) |
| 6 | 超链接 | (填) | (填) | (填) |
| 7 | 脚注 | (填) | (填) | (填) |
| 8 | 等宽字体 | (填) | (填) | (填) |
| 9 | 页眉 | (填) | (填) | (填) |

> ✅完全 / ⚠️部分 / ❌丢失

## 3. 性能数据

| 方案 | p50 (ms) | p95 (ms) | max (ms) |
|------|----------|----------|----------|
| A-POI | (填) | (填) | (填) |
| B-LibreOffice | (填) | (填) | (填) |
| C-Mammoth | (填) | (填) | (填) |

## 4. 依赖/部署对比

| 方案 | 依赖增量 | 部署要求 | 备注 |
|------|---------|---------|------|
| A-POI | ~5MB (poi-ooxml) | 纯 Java | 无外部进程 |
| B-LibreOffice | ~500MB (系统包) | 需 soffice 二进制 | 子进程每次 |
| C-Mammoth | ~30MB (npm) | 需 Node.js + npm install | 子进程每次 |

## 5. 决策建议

**主选**：(填方案 + 理由，1-3 段)
**Fallback**：(若有，描述场景)
**风险**：(已知弱点, M2-A 需绕开)

## 6. M2 启动时需复用的工作

- 同方法对 Excel/PPTX/PDF/DOC 跑一次快速评估
- 重点关注：(填 1-2 项)
```

- [ ] **Step 4: 跑测试矩阵做最后回归**

Run: `cd poc-conversion && ./mvnw test`
Expected: 全部通过（已 `@EnabledIf` 探测的方案被跳过不算失败）

- [ ] **Step 5: 提交**

```bash
cd poc-conversion
git add src/test/java/poc/converter/PerformanceBenchmarkTest.java REPORT.md
git commit -m "docs(m2-poc): 评估报告 + 性能基准"
```

---

### Task 8: README 完整化 + 全量回归

**Files:**
- Modify: `poc-conversion/README.md`

- [ ] **Step 1: 更新 README（添加已知限制）**

```markdown
# KEP M2-PoC 文档转换保真度评估

## 已知限制

- 方案 B 需宿主装 LibreOffice（`apt install libreoffice` 或 `brew install --cask libreoffice`）
- 方案 C 需宿主装 Node.js 18+ 并在 `src/main/resources/mammoth/` 跑 `npm install`
- 任一依赖缺失时该方案 `@EnabledIf` 跳过，测试矩阵仍可通过
- 真实生产路径（M2-A）将用 JODConverter 池化 LO + npm install 集成到 Docker 镜像，PoC 不做

## 复现 PoC 结论

1. `bash fixtures/generate.sh` 生成测试 docx
2. `npm install --prefix src/main/resources/mammoth`
3. `./mvnw test -Dbenchmark=true 2>&1 | grep BENCH` 跑性能
4. `./mvnw spring-boot:run -Dspring-boot.run.arguments=--server.port=18090` 启动 demo
5. 浏览器开 http://localhost:18090 上传 docx 看 3 列并排

## 报告

[REPORT.md](./REPORT.md) 给出最终推荐。
```

- [ ] **Step 2: 全量回归**

Run: `cd poc-conversion && ./mvnw test`
Expected: 全部通过

- [ ] **Step 3: 提交**

```bash
cd poc-conversion
git add README.md
git commit -m "docs(m2-poc): README 完整化 + 已知限制"
```

---

## Self-Review

- **Spec coverage** (对照 M2-PoC spec §1-9)：
  - §1 1 主格式 × 3 方案: ✓ Task 3/4/5
  - §2 测试 docx 9 特性: ✓ Task 2
  - §3 demo 架构: ✓ Task 6
  - §4 评估指标: ✓ Task 7 报告 + 性能基准
  - §5 报告结构: ✓ Task 7
  - §6 完成判据: ✓ Task 7/8
  - §7 不在 PoC 范围: ✓ 明确未做（多格式/编辑器/批量）
  - §8 不在 KEP 主仓: ✓ 独立子项目，自带 pom
  - §9 PoC 之后: ✓ 报告 §5 给出 M2-A 选型
- **Placeholder scan**: 无 TBD/TODO
- **类型一致性**: `Converter.name()` / `convert(InputStream)` 3 个实现签名一致；3 个测试类同形
- **关键调整**:
  - Task 4/5 用 `@EnabledIf` / `@EnabledIfSystemProperty` 让环境不可用时不报错——这是 PoC 的特点（环境差异大）
  - 性能基准默认禁用（`-Dbenchmark=true` 启用）——避免每次测试都跑
  - 报告数字必须用真实跑出的填，禁止编造（spec 写明）
- **前置**:
  - JDK 21 + Maven
  - Python + python-docx（生成测试 docx）
  - Node.js 18+ (Mammoth)
  - LibreOffice (LO 方案，可选)
  - 不需 Docker（独立子项目）
